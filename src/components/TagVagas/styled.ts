import styled from "styled-components";

export const VagasContainer = styled.div`
    display: flex;
    align-items: center;
    font-weight: 500;
    font-size: 12px;
    margin-top: 1em;
    gap: .5em;
`;